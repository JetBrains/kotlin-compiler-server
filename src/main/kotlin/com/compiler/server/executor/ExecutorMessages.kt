package com.compiler.server.executor

object ExecutorMessages {
  const val TIMEOUT_MESSAGE = "Evaluation was stopped as exceeding time limit"
  const val TOO_LONG_OUTPUT_MESSAGE = "Evaluation was stopped as exceeding maximum output size"
  const val NO_COMPILERS_FILE_FOUND = "No compilation files found!"
}